/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,ts,jsx,tsx}', // Ensure Tailwind is looking in your src directory
  ],
  theme: {
    extend: {
      // Add custom colors, fonts, and styles if needed
    },
  },
  plugins: [],
};

